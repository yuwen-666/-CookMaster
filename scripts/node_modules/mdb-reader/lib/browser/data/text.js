import { Database } from "../Database.js";
import { uncompressText } from "../unicodeCompression.js";
export function readText(buffer, _col, database) {
    return uncompressText(buffer, database.format);
}
